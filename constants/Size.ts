import { scale } from "react-native-size-matters";
const TextSize = {
  extraSmall: scale(10),
  small: scale(12),
  medium: scale(16),
  large: scale(20),
  extraLarge: scale(22),
  extraExtraLarge: scale(22),
};

export default TextSize;
